{
    "cmd": ["sbnf", "${file}", "${file_path}/${file_base_name}.sublime-syntax"],
    "file_regex": "^.*\\((.*):([0-9]+):([0-9]+)\\).*$",
    "working_dir": "${file_path}",
    "selector": "source.sbnf",

    "variants":
    [
        {
            "name": "Build Debug",
            "cmd": ["sbnf", "-g", "${file}", "${file_path}/${file_base_name}.sublime-syntax"],
        }
    ]
}
